set -g mouse on
set -g history-limit 100000

# Let standard Page Up / Page Down keys browse tmux scrollback without the
# prefix while still passing Page Down through to interactive apps outside copy
# mode.
bind-key -n PPage if-shell -F '#{pane_in_mode}' 'send-keys -X page-up' 'copy-mode -u'
bind-key -n NPage if-shell -F '#{pane_in_mode}' 'send-keys -X page-down' 'send-keys NPage'
bind-key -T copy-mode PPage send-keys -X page-up
bind-key -T copy-mode NPage send-keys -X page-down
bind-key -T copy-mode-vi PPage send-keys -X page-up
bind-key -T copy-mode-vi NPage send-keys -X page-down
